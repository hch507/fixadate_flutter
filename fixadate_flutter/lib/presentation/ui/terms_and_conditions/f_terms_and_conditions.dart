

import 'package:flutter/material.dart';

class TermsAndConditionsFragment extends StatelessWidget {
  const TermsAndConditionsFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(height: 400,padding: EdgeInsets.symmetric(horizontal: 30),);
  }
}
